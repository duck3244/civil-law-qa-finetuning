# requirements.txt
# 민법 QA Fine-tuning 프로젝트 필수 라이브러리 (오류 수정 버전)

# 핵심 머신러닝 라이브러리 (호환성 보장)
torch>=2.0.0,<2.5.0
transformers>=4.40.0,<5.0.0  # BitsAndBytesConfig 지원 버전
datasets>=2.14.0,<3.0.0
accelerate>=0.24.0,<1.0.0

# TRL (최신 버전으로 업데이트)
trl>=0.8.0,<1.0.0

# PEFT (Parameter Efficient Fine-tuning)
peft>=0.8.0,<1.0.0

# 양자화 라이브러리 (최신 버전)
bitsandbytes>=0.42.0

# 데이터 처리
pandas>=1.5.0
numpy>=1.24.0,<2.0.0

# 개발 및 유틸리티
tqdm>=4.65.0
requests>=2.28.0

# 선택적 라이브러리들 (주석 해제해서 사용)
# flash-attn>=2.5.0
# liger-kernel>=0.2.0

# 설치 명령어:
# pip install -r requirements.txt
# 또는 conda 환경에서:
# conda install pytorch torchvision torchaudio pytorch-cuda=11.8 -c pytorch -c nvidia
# pip install transformers datasets accelerate trl peft bitsandbytes pandas tqdm requests